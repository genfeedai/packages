export enum AssetCategory {
  LOGO = 'logo',
  BANNER = 'banner',
  REFERENCE = 'reference',
}

export enum AssetParent {
  ORGANIZATION = 'Organization',
  INGREDIENT = 'Ingredient',
  BRAND = 'Brand',
  ARTICLE = 'Article',
}
