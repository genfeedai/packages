export enum CardVariant {
  DEFAULT = 'default',
  WHITE = 'white',
  BLACK = 'black',
}

export enum CardSize {
  DEFAULT = 'default',
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  PILL = 'pill',
}

export enum CardEmptySize {
  DEFAULT = 'default',
  SM = 'sm',
  LG = 'lg',
}
