export enum CreditTransactionCategory {
  ADD = 'add',
  DEDUCT = 'deduct',
  EXPIRE = 'expire',
  RESET = 'reset',
  ROLLOVER = 'rollover',
  REFUND = 'refund',
  BYOK_USAGE = 'byok-usage',
}

export enum CreditEntityModel {
  ORGANIZATION = 'Organization',
  USER = 'User',
}
