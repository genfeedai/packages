export enum VoiceProvider {
  HEYGEN = 'heygen',
  ELEVENLABS = 'elevenlabs',
  HEDRA = 'hedra',
  GENFEED_AI = 'genfeed-ai',
}

export enum VoiceCloneStatus {
  PENDING = 'pending',
  CLONING = 'cloning',
  READY = 'ready',
  FAILED = 'failed',
}
