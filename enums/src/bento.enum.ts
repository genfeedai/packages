export enum BentoSpan {
  SINGLE = 'single',
  DOUBLE = 'double',
  FULL = 'full',
}

export enum BentoRowSpan {
  ONE = 1,
  TWO = 2,
}

export enum BentoVariant {
  DEFAULT = 'default',
  WHITE = 'white',
  BLACK = 'black',
}

export enum BentoSize {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
}
