export enum AlertFrequency {
  INSTANT = 'instant',
  HOURLY = 'hourly',
  DAILY = 'daily',
}

export enum ContentSourceType {
  QUEUE = 'queue',
  TEMPLATE = 'template',
  AI_GENERATED = 'ai_generated',
}
