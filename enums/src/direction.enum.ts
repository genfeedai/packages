export enum TrendDirection {
  UP = 'up',
  DOWN = 'down',
  STABLE = 'stable',
}

export enum ScrollDirection {
  LEFT = 'left',
  RIGHT = 'right',
}

export enum DropdownDirection {
  UP = 'up',
  DOWN = 'down',
}
