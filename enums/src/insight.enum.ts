export enum InsightCategory {
  TREND = 'trend',
  OPPORTUNITY = 'opportunity',
  WARNING = 'warning',
  TIP = 'tip',
}

export enum InsightImpact {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}

export enum ContentSuggestionType {
  TIMING = 'timing',
  FORMAT = 'format',
  TOPIC = 'topic',
  HASHTAG = 'hashtag',
  LENGTH = 'length',
}

export enum SmartAlertType {
  ANOMALY = 'anomaly',
  MILESTONE = 'milestone',
  TREND = 'trend',
  OPPORTUNITY = 'opportunity',
}

export enum SmartAlertSeverity {
  CRITICAL = 'critical',
  WARNING = 'warning',
  INFO = 'info',
  SUCCESS = 'success',
}

export enum AnomalySeverity {
  CRITICAL = 'critical',
  WARNING = 'warning',
  INFO = 'info',
}
