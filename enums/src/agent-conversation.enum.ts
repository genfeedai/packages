export enum AgentMessageRole {
  USER = 'user',
  ASSISTANT = 'assistant',
  SYSTEM = 'system',
  TOOL = 'tool',
}

export enum AgentConversationStatus {
  ACTIVE = 'active',
  ARCHIVED = 'archived',
}
